# app.py 與現有網頁版差異矩陣

此矩陣以 `APP_PY_WEB_PARITY_AUDIT.md` 的原始行為為基準。使用者指定保留「桌局設定」與「本局分析」兩頁，因此 app.py 的主畫面功能應集中實作在分析頁，而非拆為額外導覽頁。

| 原始功能 | 現有網頁狀態 | 差異 | 修正目標 |
|---|---|---|---|
| 桌號與閒／莊／和起始數 | 已有桌局設定頁 | 符合兩頁要求。 | 保留；加強非負整數與大寫桌號驗證。 |
| 閒／莊三張牌、即時計算 | 已有 | 點數與結果顯示已具備，但需以 app.py 公式驗證一致性。 | 保留並新增牌面輸入／點數／實際勝負測試。 |
| 確認後更新局數與勝負累計 | 已有 | 分析頁送出的起始局數固定為 URL 值，後續連續輸入可能未以最新記錄累計作為下一次輸入基準。 | 儲存時以最新歷程統計作為輸入基礎。 |
| 最佳公式與反打 | 已有 | 與原始 `update_formula_result` 類似。 | 保留決策、反打、公式名稱與命中資料。 |
| 事件預警 | 已有 | 已比對六項公式，但沒有 app.py 的持久事件可視化關聯。 | 保留並加上來源局數／事件資料列。 |
| 補齊歷史資料 | 缺少 | 無法依初始累計數補登缺局、閒／莊／和與對子。 | 新增同分析頁補登面板與 `supplementHistory` API。 |
| 大路圖 | 缺少 | 沒有六列大路、和局斜線、對子標記與勝方點數。 | 新增純前端 Big Road 元件，以 history 資料重現 app.py 演算法。 |
| 歷史資料表格 | 改造中 | 先前使用卡片明細；改為表格後仍未完全採用 app.py 限制。 | 固定基礎欄＋18 公式欄、最新 9 實際列、下一局列、命中數列、同步捲動。 |
| 公式格內容 | 改造中 | app.py 的實際列顯示上一局預測，命中紅底；目前還顯示額外運算值。 | 以預測作為主要單格內容；運算值只放在表格內的輔助列／提示，不改變主要 app.py 格式。 |
| 命中處理 | 部分已有 | 需完全使用 `resolve_hit_results` 對和局的歸類，而非將所有和局視為一般命中。 | 在 API 回傳每格命中狀態與命中數時使用一致函式。 |
| 歷史顯示數量 | 不符 | 現有表格會顯示所有資料；app.py 畫面固定 9 筆歷史＋下一局＋命中數。 | 表格主視窗固定 12 列；超出資料自動或手動捲動。 |
| 歷史折疊 | 缺少 | app.py 以「＋／－」收合。 | 在歷史表頭加入可存取的收合控制。 |

## 實作優先順序

第一優先是修正歷史表格資料語義與布局，其次新增補登與大路。這三者完成後，第二頁才能在行為與資訊密度上相當於 app.py 原始主畫面。
