# app.py 網頁版相容性對照

此文件以使用者提供的 `app.py` 為唯一行為對照來源，記錄 Web 版本必須保持的資料與呈現規則。原始桌面程式為單一主畫面；現行網站保留使用者指定的兩頁導航，但第二頁必須完整承接主畫面的本局輸入、補登、預測、大路與歷史資料功能。

## 1. 資料輸入與確認時機

| app.py 行為 | 原始程式位置 | 網頁版相容規則 |
|---|---:|---|
| 桌號以大寫處理，為必填欄位 | `collect_data`、`on_confirm` | 桌局設定頁與分析頁皆以大寫桌號作為資料隔離鍵。 |
| 閒／莊各至少輸入兩張牌，僅接受 1–13 | `parse_cards_input`、`on_confirm` | 儲存前驗證雙方最少兩張合法牌。 |
| 牌面即時計算點數與實際勝負 | `on_card_changed`、`update_points` | 輸入時顯示閒點、莊點和閒／莊／和實際結果。 |
| 點數採百家樂餘數 | `compute_points` | A–9 取原值，10/J/Q/K 為 0，合計取 10 的餘數。 |
| 對子取前兩張牌相同 | `is_pair` | 計算及補登均保存閒對／莊對。 |
| 確認時計算下一局局數、更新閒／莊／和累計、執行 18 個公式、保存公式與事件 | `on_confirm` | 必須依同一順序完成持久化與畫面刷新。 |

## 2. 主畫面內容（網站第二頁）

| 區塊 | app.py 對照 | 必須呈現的 Web 功能 |
|---|---|---|
| 桌局摘要 | 第 2 列 | 桌號、目前局數、閒勝、莊勝、和局。 |
| 牌面區 | 第 3–4 列 | 閒／莊三張可輸入牌、即時計算點數與實際結果。 |
| 公式最佳結果 | `update_formula_result` | 顯示最佳公式決策、反打標記、命中／分析資料。 |
| 事件預警 | `check_event` | 以前一局六個事件公式結果匹配事件紀錄，無事件時顯示「無」。 |
| 大路 | `_build_big_road`、`refresh_big_road` | 六列莊閒路，和局疊加為斜線、閒對／莊對為左右下標記、顯示勝方點數並可橫向捲動。 |
| 補齊歷史資料 | `add_supplement_history` | 依桌局初始累計數找出缺局，提供閒／莊／和與閒對／莊對，僅存結果、對子與空牌面。 |

## 3. 歷史資料表格（關鍵規則）

`refresh_history_table` 定義的原始表格不是卡片列表；它是具有固定基礎欄與所有公式欄的橫向表格。

| 欄位組 | app.py 欄位順序 | 顯示規則 |
|---|---|---|
| 基礎欄位 | 局數、閒1、閒2、閒3、莊1、莊2、莊3、閒點、莊點、結果 | 每一列為實際開出局；結果：閒藍色、莊紅色、和綠色。 |
| 公式欄位 | 正EV、真數、導數、實數、理數、HZ、點差、價值、積分、EA、VP、HD、TP、AP、和計、機率、差額、陰陽 | 完全依 `Formula.get_all_formula()`／`FORMULA_NAMES` 原始順序，不重排。 |
| 實際歷史列 | 最新 9 筆開出局 | 第 N 局公式格要使用**第 N-1 局**的公式預測，以第 N 局實際勝負驗證；非和局時命中為紅底白字。 |
| 下一局列 | 最新局數 + 1 | 灰底；填入最新一局計算出的所有公式預測。 |
| 命中數列 | 每項公式各一格 | 統計已驗證命中數，最高以紅色、最低以藍色。 |
| 捲動 | 固定可見表格高度 | 橫向表頭與內容同步捲動；表格固定顯示標題、9 筆歷史、下一局、命中數共 12 列。 |

## 4. 命中與和局規則

1. 第 N-1 局各公式的預測用來對照第 N 局實際開出結果。
2. 實際結果為閒或莊時，預測相同即命中。
3. 實際結果為和時，沿用 `Formula.resolve_hit_results` 的相鄰結果歸類規則，而非直接視為未命中。
4. 第一筆實際歷史列沒有前一局公式，公式格應為空白／破折號，不可宣稱命中或未命中。

## 5. 本次網頁重構驗收基準

- 歷史資料必須是 app.py 式單一橫向表格，不採每局卡片展開版。
- 表頭、內容列、下一局列與命中數列同時存在，且公式欄不省略。
- 結果色彩與命中紅底規則要與原始桌面程式一致。
- 最多顯示最新九筆實際歷史資料；其餘資料以表格垂直捲動存取。
- 大路與補齊歷史資料在分析頁可直接操作。
