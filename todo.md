# Project TODO

- [x] 分析 `Card.py`、`Storage.py`、`Formula.py`、`CsvManager.py`、`Base.py` 與 `app.py`，逐項記錄既有資料結構與業務規則。
- [x] 取得並核對 `BaccaratPredictor_web.html` 視覺藍圖，將其風格映射至 Web 介面。
- [x] 將 BaccaratPredictor_web.html 的色彩、卡片面板、統計資訊條、雙欄內容區與歷程彈窗對應至 Dashboard 元件與頁面樣式。
- [x] 在 `drizzle/schema.ts` 定義與原始 Python 資料結構一致的卡片、儲存空間與公式資料表。
- [x] 產生並套用 MySQL migration，確認所有核心資料均由資料庫持久化，不使用本地檔案儲存。
- [x] 在 `server/db.ts` 實作卡片的新增、讀取、更新、刪除、搜尋及分頁資料存取函式。
- [x] 在 `server/db.ts` 實作儲存空間物品的存入、取出、查詢、搜尋及分頁資料存取函式。
- [x] 在 `server/db.ts` 實作公式參數與計算紀錄的儲存、查詢、搜尋及分頁資料存取函式。
- [x] 在 `server/routers.ts` 建立與 Python 卡片業務邏輯一致的型別化 API 程序。
- [x] 在 `server/routers.ts` 建立與 Python 儲存空間業務邏輯一致的型別化 API 程序。
- [x] 在 `server/routers.ts` 建立與 Python 公式計算邏輯一致的型別化 API 程序。
- [x] 在 `server/routers.ts` 建立與 Python CSV 匯入、匯出邏輯一致的型別化 API 程序。
- [x] 補齊儲存空間 API 的依局數查詢、區間歷程、最後一筆資料、六公式事件比對及換桌重設對應行為。
- [x] 將 CSV 匯出改為符合 `CsvManager.FORMULA_FIELDS` 與 `EVENT_FIELDS` 順序及 JSON 序列化規則的 CSV 文字，並依 CSV 原始欄位值匯入。
- [x] 檢視並採用既有 `DashboardLayout` 元件，建立統一側邊欄導覽。
- [x] 建立卡片管理頁面，提供新增、編輯、刪除、瀏覽、關鍵字搜尋與分頁。
- [x] 建立儲存空間頁面，提供物品存入、取出、查詢、關鍵字搜尋與分頁。
- [x] 建立公式計算頁面，提供參數輸入、執行計算、結果顯示、歷程查詢、關鍵字搜尋與分頁。
- [x] 建立 CSV 管理頁面，提供 CSV 檔案匯入與既有資料匯出。
- [x] 依 BaccaratPredictor_web.html 建立優雅、精緻且具視覺層次的響應式設計。
- [x] 撰寫並執行 Vitest，驗證核心公式、資料驗證與 API 成功／失敗路徑。
- [x] 以預覽畫面驗證桌面與行動版佈局、表單操作、搜尋、分頁、CSV 流程及資料庫持久化。
- [x] 補上對應 BaccaratPredictor_web.html 的歷史記錄彈窗，並串接 MySQL 公式歷程。
- [x] 在儲存空間頁面加入明確的公式紀錄存入流程，串接原始 Storage.py 資料寫入契約。
- [x] 在公式儀表板加入歷程列表、關鍵字搜尋與分頁，完整提供公式資料查詢操作。
- [x] 完成桌面與行動版視覺、互動驗證後，再確認精緻響應式設計完成。
- [x] 以實機操作驗證桌面與行動版的表單提交、搜尋、分頁、CSV 匯入匯出與資料寫入後重新查詢流程。
- [x] 以實機操作驗證歷程彈窗、Storage 存入流程與公式歷程搜尋／分頁互動後，再確認互動與響應式設計完成。
- [x] 確認預覽網址、登入授權與已儲存版本，排除使用者無法查看或操作網頁版的阻塞原因。
- [x] 建立完成版本並提供可直接開啟的專案預覽入口與登入操作說明。
- [x] 明確確認目前預覽在登入前與登入後的可見狀態，並提供使用者登入操作說明。
- [x] 建立可交付 checkpoint 並提供版本入口，讓使用者可從管理介面直接預覽網站。
- [x] 移除公式儀表板的「歷程記錄」按鈕與彈窗入口。
- [x] 在本局公式計算區直接顯示 app.py 對應的下一局預測、命中次數與醒目命中資訊。
- [x] 將內嵌歷程限制為最多十二筆可見資料，超出部分改採自動捲動式列表。
- [x] 驗證桌面與行動版的歷程區排版、自動捲動行為與命中狀態顯示。
- [x] 以固定列高明確限制內嵌歷程最多十二筆可見，並保留超過資料的自動捲動。
- [x] 以超過十二筆的歷程資料驗證命中、未命中、待下一局狀態與自動捲動結果。
- [x] 將歷程列改為固定高度並精準以十二列高度限制可視區，避免內容高度隨列項變動。
- [x] 補強超過十二筆資料下的 hit、miss、pending 三種狀態與自動捲動前端行為驗證。
- [x] 新增前端單元測試，驗證超過十二筆時啟動自動捲動及 hit、miss、pending 三種歷程列樣式。
- [x] 新增內嵌歷程元件測試，掛載十三筆資料並驗證自動捲動機制與 hit、miss、pending 三種 UI 標籤及樣式。
- [x] 在掛載後的歷程 DOM 驗證 hit、miss、pending 三種狀態列的對應色彩樣式類別。
- [x] 將 Dashboard 首頁拆分為桌局設定頁與本局分析頁兩個明確步驟。
- [x] 在設定頁提供桌號、閒勝、莊勝及和局輸入，確認後導向分析頁。
- [x] 在分析頁提供閒／莊牌面輸入、最佳公式預測、事件預警與歷史資料結果。
- [x] 在分析頁加入返回按鈕，清除該桌全部公式紀錄但保留事件紀錄後回到設定頁。
- [x] 撰寫並執行雙頁導覽與只清除公式紀錄的測試，並驗證桌面與行動版流程。
- [x] 新增設定頁確認後導向分析頁並帶入桌局查詢參數的前端整合測試。
- [x] 新增分析頁返回時呼叫 resetFormulaTable、保留事件紀錄並導回設定頁的前端整合測試。
- [x] 逐項比對 `Formula.py` 與 `server/baccarat.ts` 的十八項公式、歷史分析、命中判定與最佳公式決策規則。
- [x] 修正 Web 版公式計算與最佳公式流程，使回傳欄位、決策順序及反打規則符合 `Formula.py`。
- [x] 建立以同一批資料交叉驗證 Python 與 Web 版公式、分析及最佳公式輸出的測試。
- [x] 驗證修正後的最佳公式結果會正確顯示於分析頁並交付更新版本。
- [x] 重新驗證分析頁的最佳公式、反打標示、事件預警與歷程摘要，保留桌面或行動版的可視證據。
- [x] 建立 Formula.py 相容修正版 checkpoint，提供可直接預覽的交付版本入口。
- [x] 設計自訂公式管理頁示意圖，呈現 Formula.py 範本、完整運算式、條件預測與自訂公式刪除操作。
- [x] 產出高保真示意圖並取得使用者確認，再開始實作自訂公式功能。
- [x] 確認自訂公式管理頁示意圖檔案已完成產出、可正常開啟，並以有效附件重新交付。
- [x] 以已驗證的 PNG 附件重新交付自訂公式管理頁示意圖，並請使用者確認可正常開啟。
- [x] 修訂自訂公式示意圖，加入開出閒／莊的實際點數、權重、加減乘除、點數權重與結果條件等完整運算元素。
- [x] 重新產出並驗證可開啟的修訂示意圖，供使用者確認完整自訂公式規則結構。
- [x] 以有效 PNG 附件交付包含開出點數、權重與四則運算的修訂示意圖，供使用者確認。
- [x] 修訂示意圖，加入逐局實際開出紀錄、每局閒點／莊點／勝方與可引用的歷史區間資料來源。
- [x] 在自訂公式規則中明確示範引用實際開出點數與其權重、運算及條件預測的完整寫法。
- [x] 修訂示意圖，加入 A～K 牌面群組權重、閒點／莊點獨立映射權重表與卡牌加權合計設定。
- [x] 以 VP 邏輯示範 `p1 / p2`、`b1 / b2`、安全除法、差值及閒／莊／和預測的規則預覽。
- [x] 以已驗證的 PNG 附件交付 VP 卡牌權重、點數映射與三態預測版示意圖，供使用者確認。
- [x] 依 `Formula.py` 十八項公式整理可配置的資料來源、映射、運算、除法、條件與預測規則元件，並建立 `formula-component-mapping.md` 對照文件。
- [x] 依使用者後續確認，改為不採用 Formula.py 範本起始流程，而由資料來源自動新增規則。
- [x] 製作並交付全公式相容的自訂公式管理頁示意圖，供確認後再實作。
- [x] 重做自訂公式頁示意圖，將 Formula.py 範本流程改為依資料來源自動新增規則的步驟式流程。
- [x] 在示意圖中呈現牌面點數、閒點數、莊點數、局數與前一局資料各自展開的設定項目、加減乘除與最終預測條件。
- [x] 修訂自動新增流程，使閒牌面／莊牌面權重可選擇共用或各自獨立設定，並在畫面中以分離面板呈現。
- [x] 在前一局資料設定加入閒莊點數、指定牌面與前局閒莊差額等運算來源，支援加減乘除及差額絕對值。
- [x] 修訂前一局資料面板，使來源 A 與來源 B 可分別選擇前局閒／莊點數或閒／莊指定第幾張牌。
- [x] 在前一局資料面板加入差額、相乘與四則運算選擇器，並以組合結果銜接最終預測條件。
- [x] 定義自訂公式 JSON 規則契約，涵蓋共用／獨立閒莊牌面權重、閒莊點數權重、局數、前一局雙來源運算與三態預測條件。
- [x] 建立 MySQL 自訂公式資料表與 migration，保存使用者的公式名稱、規則、啟用狀態及建立時間。
- [x] 實作安全的自訂公式運算器，不執行任意程式碼，僅處理白名單資料來源、四則運算、差額絕對值與預測條件。
- [x] 建立自訂公式的新增、列表、啟用切換、刪除及預覽 tRPC 程序。
- [x] 建立自訂公式管理頁，支援閒莊牌面權重共用／獨立切換、前一局雙來源運算器、預測條件及公式清單操作。
- [x] 在本局分析頁顯示啟用中的自訂公式預測結果。
- [x] 撰寫並執行自訂公式規則、資料庫持久化、前局差額／相乘與前端操作流程測試。
- [x] 修正自訂公式累積運算語義，讓第一個啟用來源在乘除情境可作為正確起始值，並補上乘除鏈式測試。
- [x] 補強自訂公式預覽與分析整合測試，覆蓋乘法、除法、絕對差額與前局雙來源案例。
- [x] 將已建立公式的刪除操作明確標示為「刪除新公式」，並提供管理頁規則與列表讀取失敗狀態。
- [x] 新增實際 customFormulas 資料持久化測試，驗證建立、列表、啟用切換與刪除會反映於 MySQL 資料庫狀態。
- [ ] 匯出 MySQL 資料庫結構與目前資料為可還原 SQL 檔，且不含機密設定。
- [ ] 建立排除 `.env`、登入憑證、建置產物及相依套件的完整原始碼封包，附上本機還原說明。
- [ ] 驗證封包內容、壓縮檔完整性與資料庫匯出檔後交付下載連結。
- [x] 移除公開 Render 版本的 Manus OAuth 登入畫面與登入導向，改為免登入即可使用。
- [x] 將資料存取改為使用固定的公開工作階段使用者，避免不同訪客讀取既有匯入使用者的桌局、公式與事件紀錄。
- [x] 保留且簡化為兩頁流程：桌局設定頁（桌號、閒勝、莊勝、和局）與本局分析頁。
- [x] 更新前端路由與分析頁返回行為，確保返回設定頁時只清除該公開工作階段的本局公式記錄。
- [x] 補齊免登入模式與雙頁流程的 Vitest 覆蓋，完成建置與公開 Render 健康檢查驗證。
- [x] 依 app.py 的逐局結果格式擴充「歷史資料結果」，顯示使用者輸入的閒／莊牌面與計算後點數。
- [x] 在每筆歷史資料呈現實際勝負、全部 Formula.py 公式的預測與計算結果（以格內預測、命中狀態與滑鼠停留的運算值呈現），以及最佳公式的反打與命中狀態。
- [x] 補齊完整歷史結果格式化與展開互動的 Vitest，重新驗證 TypeScript、建置與公開 Render 頁面。
- [x] 將卡片式完整歷史明細改為 app.py 相容的橫向可捲動表格，固定顯示局數、閒／莊三張牌、閒點、莊點與實際結果。
- [x] 依 Formula.py 的原始順序將十八項公式置於表格後方欄位，每個儲存格呈現預測與命中狀態，並保留運算值提示。
- [x] 補齊 app.py 表格標題、公式欄順序、醒目命中與下一局／命中數列的前端回歸測試並完成 Render 驗證。
- [x] 逐段解析 app.py 的畫面配置、桌局初始化、牌面輸入、即時計算、確認儲存、事件預警、補齊歷史資料、大路與歷史表格流程。
- [x] 建立 app.py 與網頁版的可驗證差異清單，依原始程式語義修正互動時機、欄位、排序、滾動與醒目規則。
- [x] 完成 app.py 相容介面的實作、回歸測試、公開 Render 驗證與對照紀錄。
- [x] 移除分析頁的大路圖及其不再使用的前端元件，保留兩頁流程與其他 app.py 相容資料功能。
- [x] 重新驗證移除大路圖後的桌局設定、分析、補齊歷史與橫向歷史表格流程。
- [x] 將歷史資料區改為僅保留單一「歷史資料」標題，移除重複的「歷史資料表格」標題與副標題。
- [x] 將啟用中的自訂公式預測整合至同一個歷史資料區塊，取消獨立的自訂公式預測面板。
- [x] 以網頁式卡片、區段與可展開公式結果取代傳統橫向表格，保留逐局輸入、點數、勝負、公式預測與命中資訊。
- [x] 補齊整合歷史資料區的 Vitest 並完成 TypeScript、生產建置與公開 Render 驗證。
- [x] 建立後台專用帳號密碼資料與安全雜湊，禁止未驗證請求讀取後台資料。
- [x] 實作後台登入、登出、目前工作階段檢查與後台內修改密碼的受保護 API。
- [x] 建立後台登入頁與依參考畫面設計的側邊欄儲存空間頁面，讀取 MySQL 公式紀錄與事件儲存資料。
- [x] 後台提供儲存空間公式紀錄／事件儲存分頁、搜尋與取出操作，並保留原有資料庫內容。
- [x] 補齊後台權限拒絕、登入、密碼變更與儲存空間讀取的 Vitest，完成公開 Render 安全驗證。
