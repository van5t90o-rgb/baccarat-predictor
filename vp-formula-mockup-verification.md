# VP 自訂公式示意圖核對

已檢視 `custom-formula-management-vp-mapping-mockup.png`。頁面左側的 **A～K 牌面權重設定** 顯示三組規則：`A、2、3、4、5 → +1`、`6、7、8、9 → +2`、`10、J、Q、K → +3`；並以閒牌 `A、5、8` 與莊牌 `7、Q、2` 示範加總得到 `p1=4` 與 `b1=6`。

頁面中段的 **閒／莊點數映射權重** 分別呈現 `p_map` 與 `b_map`。VP 流程明確列出 `v1 = safe_divide(p1, p2)`、`v2 = safe_divide(b1, b2)`、`差值 = round(v1 - v2, 2)`；底部預測條件含 `v1 > v2 → 閒`、`v1 < v2 → 莊`、`v1 = v2 → 和`。右側預覽以 `v1=2.86`、`v2=3.53`、差值 `-0.67` 顯示最終預測為莊。

自訂公式列含可見的 `刪除新公式` 按鈕，符合使用者的刪除需求。
